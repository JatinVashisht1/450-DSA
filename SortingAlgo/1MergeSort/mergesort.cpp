#include<iostream>
using namespace std;

// i: smallest unpicked in left array
// j: smallest unpicked in right array
// k: position needs to be filled in array A

void merge(int* A, int mid, int low, int high){
    int i = low;
    int j = mid+1;
    int k = low;
    int B[100];
    while(i <= mid && j<=high){

    }
}

int main(){

return 0;
}